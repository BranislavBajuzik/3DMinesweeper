# Called by
# - 13 48 -1

tellraw @p ["\nThe goal of Minesweeper is to find all mines. To help you with that, each tile has a number on it, representing how many mines are around it (except for mines and empty tiles)."]
